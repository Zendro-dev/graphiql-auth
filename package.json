{
  "name": "graphiql-auth",
  "private": true,
  "version": "0.1.0",
  "description": "Standalone GraphiQL deployment for Zendro's graphql-server, with OAuth2 login and jq/JSONPath meta-query filtering",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:graphiql": "npm --prefix zendro-graphiql run build",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "zendro-graphiql": "file:zendro-graphiql"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
